macumbaria-postgresql:
  container_name: macumbaria-postgresql
  image: postgres:9.4.5
  # volumes:
  #   - ~/volumes/jhipster/macumbaria/postgresql/:/var/lib/postgresql/
  environment:
    - POSTGRES_USER=macumbaria
    - POSTGRES_PASSWORD=
  ports:
    - "5432:5432"
